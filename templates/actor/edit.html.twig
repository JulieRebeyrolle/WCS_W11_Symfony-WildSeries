{% extends 'base.html.twig' %}

{% block title %}{{ 'actor.edit-actor'|trans }}{% endblock %}

{% block body %}
    <h1 class="text-center mt-5">{{ 'actor.edit-actor'|trans }}</h1>

    <div class="container">
        <a href="{{ path('actor_index') }}" class="btn-primary btn my-5">{{ 'nav.actor-list'|trans }}
        </a>

        {{ include('actor/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="text-right">
            {{ include('actor/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
